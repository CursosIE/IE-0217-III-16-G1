------ LABORATORIO 3 - G1 -----

ESTRUCTURAS DE DATOS NO LINEALES:

	Organizacion de directorios:
		src ->bst: ubicacion de archivos propios de arbol de busqueda binario
		    ->garray: ubicacion de archivos propios del grafo implementado con matriz de adyacencia
		    ->gpointer: ubicacion de archivos propios del grafo implementado con punteros
		    
		include: archivos include como list.h y listWithArray.h que se usan en archivos en src
		build: aqui esta el makefile y aqui caen los ejecutables.

	Para compilar y correr este programa:

		1) Se debe ingresar a la carpeta build
		2) Desde la linea de comandos se debe compilar el programa con el Makefile 

		>> make nolineal
		
		se cren tres ejecutables: gwa, gwp, bst

		3) Se corren los archivos ejecutables:

		>> make run

		4) Se limpian los archivos intermedios y ejecutables con

		>> make clean
